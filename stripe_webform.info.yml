name: 'Stripe webform'
type: module
description: 'Provides a stripe webform element and default handlers'
core_version_requirement: ^10 || ^11
package: Stripe
dependencies:
  - stripe
  - webform:webform
